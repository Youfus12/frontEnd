/*
----Introduction-----
//Print with console.log(''), or "" or better use ``: for variables
console.log('We print with console.log()')

//print Alert on the website
window.alert("this is an alert")

//Here we will select the element in html, and change textContent
document.getElementById("myH1").textContent = "Hello from document.getElementByID fonction h1 html"
document.getElementById("myP").textContent = "Hello from same fonction with p html"

*/

//-----Variables----
/*
1. declaration: let x;
2. asigment : x=10;

//Same syntax for strings/booleans
let x=101;

//This will execute in the consol of the website
console.log(`The type of the last variable ${typeof x} and its value ${x}`);

//This will select the html elemnt and change its textContent
let text=`The type of the this variable ${typeof x} and its value ${x}.`;
document.getElementById("myH1").textContent = text;

*/




